#!/usr/bin/node
// This code prints lines using string array and loop.

const arr = ['C is fun', 'Python is cool', 'JavaScript is amazing'];

for (let i = 0; i < arr.length; i++) {
  console.log(str[i]);
}
